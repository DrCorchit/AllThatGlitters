#!/usr/bin/env bash
git pull
echo "Updated server code; building  server."
sudo mvn clean package || exit 1
sudo cp target/FileServer-*.jar target/server.jar || exit 1
echo "Starting Server."
sudo java -jar target/server.jar &